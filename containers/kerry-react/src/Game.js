import React, {useState} from 'react';

export function Game() {
  const [state, setState] = useState({foo: 'bar'});
  //setState({foo: 'baz'});
  return (
    <div>
      GAME DATA??
    </div>
  );
}
